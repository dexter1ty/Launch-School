/*

Log all even numbers from 1 to 99, inclusive, to the console, with each number on a separate line.

*/

function evenList(limit) {
  for (let i = 0 ; i <= limit ; i += 2) {
    console.log(i);
  }
}
